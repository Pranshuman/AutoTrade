# Nixpacks configuration for Railway
# Bun includes Node.js compatibility, so we only need Bun

[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "bun run server.ts"

