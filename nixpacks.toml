# Nixpacks configuration for Railway
# This ensures Bun is used as the runtime

[phases.setup]
nixPkgs = ["nodejs-20_x", "bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "bun run server.ts"

